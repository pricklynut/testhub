{% extends 'base.html.twig' %}

{% block body %}

    {{ parent() }}

    <div class="wrapper container">
        <div class="row">
            <div class="content col-md-9 col-sm-8 col-xs-12">
                <h1 style="margin-bottom: 20px;">Попробовать свои силы</h1>
                <div class="tests panel panel-default">

                    <ul class="list-group">
                    {% for item in tests %}
                        <li class="test-item list-group-item"
                             style="box-sizing:content-box;height: 66px;padding:10px">
                            <div class="test-title-tags pull-left col-md-10">
                                <h3 class="title" style="margin-top:10px;margin-bottom: 2px;">{{ item.title }}</h3>
                                {% if item.tags is not empty %}
                                    <div class="tags">
                                        {% for tag in item.tags %}
                                            <span class="tag-item">
                                                <a href="{{ path('homepage') }}"
                                                >#{{ tag.name }}</a>
                                            </span>
                                        {%  endfor %}
                                    </div>
                                {% endif %}
                            </div>
                            <div class="test-start pull-right col-md-2">
                                <div style="line-height: 66px;text-align: center;">
                                <a href="{{ path('homepage') }}">
                                    <span class="badge"
                                          style="font-size: 28px;">&rarr;</span>
                                </a>
                                </div>
                            </div>
                        </li>
                    {% endfor %}
                    </ul>

                </div>

                <p class="lead">
                    <a href="{{ path('homepage') }}">Смотреть все тесты &rarr;</a>
                </p>
            </div>
            <div class="sidebar col-md-3 col-sm-4 col-xs-12">
                <h2>О сайте</h2>
                <p>
                    TestHub это сервис, который позволяет вам легко создавать
                    тесты для проверки знаний и просматривать результаты в
                    удобном интерфейсе.
                </p>
                <p>
                    Для создания и прохождения теста не требуется регистрация,
                    но мы советуем это сделать, так как в этом случае вы легко
                    сможете управлять своими тестами.
                </p>
                <p>
                    Присоединяйтесь к сообществу TestHub!
                </p>

                <p>
                    <a href="{{ path('homepage') }}"
                       class="btn btn-default btn-lg">Создать тест</a>
                </p>
                <p>
                    <a href="{{ path('homepage') }}">Зарегистрироваться</a>
                </p>
            </div>
        </div>
    </div>

{% endblock body %}
